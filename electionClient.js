const FileReader = require('./FileReader.js');
const electionParser = require('./electionParser.js');

let list = FR.LoadFileIntoArrayByLine('electionSimplified.csv');

function statesWithAtLeast(list){
  let leastlist = [];
  for(i=0;i<list.length;i++){
    
  }
}
